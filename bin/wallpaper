#!/bin/bash

PROJECT_DIR="$HOME/Nobara_FF_UI"
DIR="$PROJECT_DIR/assets/wallpapers/On_Desktop"

FILE=$(find "$DIR" -type f -name "*.mp4" | head -n 1)

[ -z "$FILE" ] && exit 1

pkill mpvpaper 2>/dev/null

mpvpaper -o "no-audio loop hwdec=auto" "*" "$FILE"